{
  "docker-compose": {
    "command": "cd nemo-microservices-quickstart_25.12 && docker-compose up -d",
    "working_dir": "/app",
    "env_var": "NGC_API_KEY"
  },
  "helm": {
    "command": "helm install nemo-microservices ./nemo-chart --set imagePullSecrets[0].password=$NGC_API_KEY",
    "working_dir": "/app",
    "env_var": "NGC_API_KEY"
  }
}
